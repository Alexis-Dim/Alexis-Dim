<svg width="800" height="350" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 800 350">
    
    <!-- Fixed 7x53 grid with same styling -->
    <g id="fixed-grid">
        <!-- Draw 7 rows of 53 boxes each -->
        <script type="text/javascript">
            // SVG namespace
            const svgNS = "http://www.w3.org/2000/svg";
            
            // Grid dimensions
            const rows = 7;
            const cols = 53;
            
            // Box dimensions
            const boxWidth = 10;
            const boxHeight = 10;
            
            // Grid spacing
            const xGap = 2;
            const yGap = 2;
            
            // Starting position
            const startX = 85;
            const startY = 12;
            
            // Create each box in the grid
            for (let row = 0; row < rows; row++) {
                for (let col = 0; col < cols; col++) {
                    // Calculate position
                    const x = startX + col * (boxWidth + xGap);
                    const y = startY + row * (boxHeight + yGap);
                    
                    // Create box
                    const rect = document.createElementNS(svgNS, "rect");
                    rect.setAttribute("x", x);
                    rect.setAttribute("y", y);
                    rect.setAttribute("width", boxWidth);
                    rect.setAttribute("height", boxHeight);
                    rect.setAttribute("fill", "#151b23");
                    rect.setAttribute("rx", "3");
                    rect.setAttribute("ry", "3");
                    rect.setAttribute("stroke", "#ffffff0d");
                    rect.setAttribute("stroke-width", "1");
                    
                    // Add to SVG
                    document.querySelector("svg").appendChild(rect);
                }
            }
        </script>
    </g>
    
    <!-- Green boxes with animations (using the same animation style) -->
    <rect x="98" y="26" width="10" height="10" fill="#39d353" rx="2" ry="2">
        <animate 
            attributeName="opacity" 
            values="1;1;0;0;1;1" 
            keyTimes="0;0.034;0.04;0.99;1;1" 
            dur="15s" 
            begin="0s" 
            repeatCount="indefinite" />
    </rect>
    
    <rect x="146" y="38" width="10" height="10" fill="#39d353" rx="2" ry="2">
        <animate 
            attributeName="opacity" 
            values="1;1;0;0;1;1" 
            keyTimes="0;0.07;0.08;0.985;1;1" 
            dur="15s" 
            begin="0s" 
            repeatCount="indefinite" />
    </rect>

    <rect x="194" y="74" width="10" height="10" fill="#39d353" rx="2" ry="2">
        <animate 
            attributeName="opacity" 
            values="1;1;0;0;1;1" 
            keyTimes="0;0.1;0.11;0.98;1;1" 
            dur="15s" 
            begin="0s" 
            repeatCount="indefinite" />
    </rect>
</svg>
